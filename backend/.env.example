# Local development only. The server does not load this file automatically;
# export these variables or use your preferred environment loader.
NODE_ENV=development
PORT=3001
APP_ORIGIN=http://localhost:3000
JWT_SECRET=replace-with-at-least-32-random-characters

# Optional. Relative paths resolve from the directory where Node is started.
# DATA_FILE=./data.json
